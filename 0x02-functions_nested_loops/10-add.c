#include "main.h"
/**
 * add - sum the two intergers passed as argument
 * @n: interger passed as argument
 * @p: interger passed as argument
 * Return: the sum of the two integers
 */

int add(int n, int p)
{
	return (n + p);
}

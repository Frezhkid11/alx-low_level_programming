#include "main.h"
/**
 * mul - multiplies two intergers
 * @a: argument to the function
 * @b: argument to the unction
 * Return: the product of the two argument
 */
int mul(int a, int b)
{
	return (a * b);
}
